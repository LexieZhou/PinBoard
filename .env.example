# Copy to .env and fill in. Vite inlines these at build time, so they end up
# in the bundle — fine for a coursework extension, NOT safe for public release.

VITE_TRITON_API_KEY=sk-your-tritonai-key
VITE_GOOGLE_MAPS_API_KEY=your-google-places-api-key
