# PinBoard Clipper：竞品分析与上架 Chrome Web Store 路线图

## 背景

PinBoard Clipper 目前已是可用的 MVP：侧边栏、TritonAI 扫页、Google Places 地理编码、Leaflet 地图、多列表、KML 导出。用户希望：(a) 摸清市面竞品，(b) 把它作为一个真正的 Chrome Web Store 扩展上架（免费、作品集定位、不做付费），(c) 决定要加哪些功能才能比现有方案真的更好。受众继续聚焦旅行规划。

这是一份策略与路线图文档，不是单个功能的实现计划。它指出上架前必须解决的拦路问题、相对竞品的护城河、以及分阶段的功能路线图。每个阶段都可独立上线。

---

## 1. 竞品全景（截至 2026 年 5 月）

### 直接竞品（同样的"从网页提取地点 → 地图"概念）

| 产品 | 用户数 | 评分 | 实现方式 | PinBoard 可利用的弱点 |
|---|---|---|---|---|
| **[MapSnap.ai](https://chromewebstore.google.com/detail/mapsnapai-travel-itinerar/apdljgemecnmhhmonanadpegcnegmanl)** | 447 | 3.7★ | 粘贴文本（博客/PDF/ChatGPT）→ AI 提取 → Google Maps 列表。免费版限制 15 个地点。 | 只能粘贴（不能自动扫页）；15 个地点的硬上限；没有页面内集成；没有展示分类；丢失来源链接。 |
| **[Map Your Travel](https://chromewebstore.google.com/detail/map-your-travel/oefngoaodhdbpdhldnmapjfkmkamgfce)** | 74 | 5.0★（2 个评分） | 自动识别任意网页上的地点，在 Google Map 上显示。 | 用户基数极小；定位只针对"餐厅"；没有列表管理；没有导出。 |
| **[Wanderlog Chrome 扩展](https://chromewebstore.google.com/detail/wanderlog-search-save-goo/ppklhnpdbmedfmgidedifnpdfcdlfijn)** | 40,000 | 2.5★ | 在文章中搜索并保存地点到 Wanderlog 行程；Airbnb 价格叠加显示。 | **评价显示：登录死循环、认证失效、几乎只在 Wanderlog 自家页面能用。** 4 万被坑的用户正在找替代品，可以直接迁移过来。 |
| **[Pin Drop](https://chromewebstore.google.com/detail/pin-drop/bnljncmnplmokllnmagkgjjjmfedbeob)** | 3,000 | 5.0★（1 个评分） | 在任意页面手动落 pin 到私人地图。加密、可离线。 | 纯手动（没有 AI 提取）；没有分类；没有来源上下文。 |

### 旁支 / 相邻方案
- **[Travel Mapper](https://chromewebstore.google.com/detail/travel-mapper-save-travel/jeegejabenlmpidohngdaafimapnkiab)** —— 右键选中文本 → 存到 Google Sheets 一行。没有地图 UI，没有 AI。
- **Notion Web Clipper** —— 通用剪藏，没有地理功能。
- **Wanderlog 网页端**（Pro $40/年） —— 那个坏掉的扩展背后的完整产品，有行程日期、日程编排、协作、移动端。**这才是 PinBoard 真正要追赶的功能天花板。**

### 这意味着什么
AI 提取地点这个细分市场**总用户不到 500**，而且**做得都很差**。Wanderlog 那 4 万用户被困在一个坏掉的扩展里。策略很清晰：PinBoard 现有的差异点已经足够有意义，一个打磨过的免费、聚焦旅行的版本可以从两头同时抢用户。

---

## 2. PinBoard 已有的护城河（已实现但被低估）

这些已经在代码里了，但在 UX 和宣传文案里应该突出讲：

1. **一键自动扫页** —— 不需要复制粘贴（vs MapSnap）
2. **AI 分类 + emoji 标签** —— 🍽 食 / 🏨 住 / 🎯 景 / 📍 其他（vs 无差别的 pin）
3. **区域上下文用于精准地理编码** —— 自动区分东京和洛杉矶的"大黑屋"
4. **多列表共享同一个 pin（合并 tag，不重复）** —— 把同一个地方再次加到第二个列表时，合并标签而不是产生重复 pin。**所有竞品都没做。**
5. **保留来源 URL + 上下文片段** —— 每个 pin 都记得*你在哪儿看到的*以及*文章是怎么说的*。Wanderlog 和 MapSnap 都把这部分丢了。
6. **隐私优先、本地存储** —— `chrome.storage.local`，无账号、无埋点。和 Pin Drop 的定位一致。
7. **完全免费、无上限** —— vs MapSnap 的 15 个地点免费门槛。

---

## 3. 上架拦路问题（Phase 0 —— 上 Web Store 前必须做完）

这些不是可选项，没做完不该把扩展发出去。

### 3.1 API Key 处理 —— `src/background/service-worker.ts`
现在 `VITE_TRITON_API_KEY` 和 `VITE_GOOGLE_PLACES_KEY` 是在构建时被打进 bundle 的。直接上 Chrome Web Store 会把这两把 key 泄露给所有下载 .crx 的人。**对作品集定位来说，两个可接受的方案：**

- **推荐：BYOK（用户自带 key）** —— 加一个 Options 页，用户自己贴 Google Places 的 key 和（可选的）TritonAI 或 OpenAI key。存到 `chrome.storage.local`，service worker 在请求时读取。零基础设施成本，零被滥用风险。新手上路体验会差一点，但作品集定位完全能接受 —— `gh` CLI、Raycast 扩展等开发者工具就是这么做的。
- 备选：Vercel/Cloudflare Workers 起一个带限流的薄代理。工作量大、有持续成本、但用户上手更顺。**作品集范围内不建议做。**

关键文件：
- `src/background/service-worker.ts` —— 把 `import.meta.env` 读取改成 `chrome.storage.local.get`
- 新增 `src/options/` —— React 写的 Options 页，通过 `manifest.json` 里的 `options_page` 注册
- `src/lib/triton.ts:1-30`、`src/lib/places.ts:1-30` —— 把 key 改成参数传入，而不是读环境变量

### 3.2 Chrome Web Store 上架素材
- 隐私政策 URL（扩展用了 storage + 远程 API 就必须有）。挂在 GitHub Pages 上即可。
- 1280×800 宣传截图（至少 1 张）
- 440×280 小宣传图
- 128×128 图标（已有）
- 详细描述
- 30 秒演示视频（Loom 录一个就行）
- 给审核员看的 `permissions` 说明文本：解释为什么需要 `scripting`、`storage`、`sidePanel`、`<all_urls>`

### 3.3 Manifest 和权限收敛
- `manifest.json` 现在申请了非常宽的 host 权限。为了过审，把页面访问改成 `activeTab` + 显式白名单（`https://tritonai-api.ucsd.edu/*`、`https://places.googleapis.com/*`）。扫页是用户主动触发的，`activeTab` 就够用。
- 加 `default_locale` 和 `_locales/en/messages.json`，为以后做国际化留口。

### 3.4 错误体验 —— `src/popup/App.tsx`、`src/lib/places.ts`
现在某个地点 geocode 失败会被静默丢掉（见 `src/lib/places.ts:61` 的 `console.warn`）。给真实用户用，必须：
- 在预览列表里标出失败的项（"⚠ 没找到这个地点"）
- 失败项旁边给一个重试按钮
- TritonAI 返回零个地点时，弹出 banner 提示"换一篇页面试试"或"试试右键 pin 选中文字"
- 第一次安装的空状态展示：示例 URL，或一个动图教程

### 3.5 新手引导流程
首次安装时：
1. 侧边栏弹出欢迎页
2. 引导用户填 API key（BYOK 路径），附上 TritonAI / Google Cloud Console 的帮助链接
3. 提供一个"用这篇示例博客试试"按钮，点击打开一篇已知能正常解析的旅行文章

---

## 4. Phase 1 —— 抓取入口扩展（让 pin 的来源更丰富）

下面每一条都对应着竞品没做好的某个点。按"投入产出比"排序。

### 4.1 右键菜单："Pin 选中的文字"
`Proposal.md` 里早就列了这个目标，一直没做。这是单一最大的 UX 提升：用户在任意句子里选中"Bar Trench"，右键 → 立刻 pin，不用扫整页。复用现有的 geocoding 流水线。要改的：
- `manifest.json` —— 加 `"contextMenus"` 权限
- `src/background/service-worker.ts` —— `chrome.contextMenus.create({id: "pin-selection", title: "Pin '%s' to PinBoard", contexts: ["selection"]})` + handler 调 `src/lib/places.ts` 的 `geocodePlaces([{name: info.selectionText, region: ""}])`

### 4.2 键盘快捷键
`manifest.json` 加 `commands`：`Cmd+Shift+P` 开侧栏 + 自动扫当前页；`Cmd+Shift+L` pin 选中文字。5 行代码的事。

### 4.3 YouTube 字幕扫描
**巨大的未挖掘金矿** —— 旅行 vlog 一集能提到 10+ 个地方。当前页是 `youtube.com/watch?...` 时，通过 YouTube 的 timedtext 接口拉自动字幕（或者爬字幕面板 DOM），喂给 TritonAI。新文件 `src/lib/youtube.ts`，在 service worker 里通过 `tab.url` 判断。

### 4.4 自动检测 + 角标计数
被动扫当前页（debounce + 用 URL 启发式或第一段 LLM 判断筛选"可能是旅行类页面"），然后在工具栏图标上显示数字角标："这页找到 12 个地点"。点击打开查看。这是**被动发现** —— 来自 Honey 和 Pruvo 的杀手 UX，应用到地点场景。
- 谨慎实现 —— 必须给一个"在这个网站禁用"开关。每页都偷偷扫，会引发隐私和 API 成本的担忧。

### 4.5 PDF 和截图支持
- PDF：拖进侧栏 → 用 `pdf.js` 抽文字 → 走现有流水线
- 截图：拖进图片 → 走视觉模型（TritonAI 可能没有，gpt-4o-mini 兜底）。对那些喜欢把 Instagram 截图存下来的旅行者非常有用。

### 4.6 ChatGPT / Claude 对话扫描
当前页是 `chat.openai.com` 或 `claude.ai` 时，扫可见对话。**几乎零成本** —— 现有的文本提取器本来就拿到了页面文本，只需要在这种 URL 下换一下 UX 文案（"从这段对话里 pin 地点"）。

---

## 5. Phase 2 —— 旅行规划深度（让列表变成行程）

到这一步，PinBoard 不再只是个剪藏工具，开始真正和 Wanderlog / Roadtrippers 正面竞争。

### 5.1 带日期的行程列表
扩展 `src/lib/types.ts` 里的 `List` 类型，加 `startDate?`、`endDate?`、`destination?`。`ListBar.tsx` 的胶囊上展示日期。按日期筛 pin。

### 5.2 按天编排日程
在有日期的列表里，让用户把每个 pin 分配到某一天（从"未排期"拖到"第 2 天"）。新视图 `src/popup/ItineraryView.tsx`，地图旁边加一个 tab 切换。`Pin` 加可选字段 `dayIndex?: number`。

### 5.3 路线优化 + 通勤时间
- 用 Google Directions API（已经申请过 Google 项目，做 Places 的时候开过）计算同一天相邻 pin 之间的步行/驾车时间。
- "优化顺序"按钮 —— 最近邻启发式重排 pin，最小化当天总路程。顶部显示当天总距离。

### 5.4 用 Google Places 丰富 pin 信息
现在 Places 调用只取 `id`、`displayName`、`formattedAddress`、`location`。把 `src/lib/places.ts:35` 的 `X-Goog-FieldMask` 扩展，加上 `rating`、`priceLevel`、`regularOpeningHours`、`primaryType`、`websiteUri`、`nationalPhoneNumber`、`photos`。在 pin 弹窗里展示。按"现在营业中"、"评分 ≥ 4"等筛选。

### 5.5 笔记 + 已访问状态
Pin 弹窗里加文本框给用户写自己的笔记。已访问 / 想去 的布尔。地图按状态筛。`Pin` 类型加字段 —— `pin-store.ts` 里做一个小的 schema migration。

---

## 6. Phase 3 —— 分享与同步（不要后端的协作）

目标：让旅伴能看到 / 贡献某个行程，但 **PinBoard 自己不需要跑后端**。和作品集定位对齐。

### 6.1 通过 URL 分享
"分享这个列表" → 生成一个可分享 URL，把 gzip + base64 编码的列表 JSON 塞进 hash fragment。对方在装了 PinBoard 的浏览器里打开 URL，看到只读视图 + "导入到我的 PinBoard" 按钮。**完全不需要服务器**。hash 永远不会被发到服务器。限制：约 30 个 pin（URL 长度上限）。

### 6.2 导出 / 导入 JSON
KML 导出的补充。让两个 PinBoard 用户通过邮件 / Slack / AirDrop 交换完整列表。在列表管理 UI 加按钮。

### 6.3 可选：通过 Gist / Google Drive 云同步
超过 URL 分享大小的行程，让用户把单个列表同步到自己的 Google Drive（用 `chrome.identity.getAuthToken` 做 OAuth）或公开 / 私密的 GitHub Gist。每个用户自己出存储，不花一分钱。每个列表单独选择是否启用同步。

### 6.4 二维码看手机版
"在手机上看" → 生成二维码指向一个静态托管的阅读页（`pinboard-share.vercel.app/#<encoded-list>`），用 Leaflet 渲染 pin。只读，但用零基础设施解决了"想在东京边走边看自己的规划"这个核心痛点。

---

## 7. 明确排除（作品集定位的边界）

下面这些很诱人，但**除非项目转向商业化，不要碰**：

- 账号系统 / 用户认证
- 实时多人协作（需要 CRDT + websocket 后端）
- 原生移动应用（PWA + 二维码分享是替代方案）
- 邮件转发收件（像 TripIt —— 需要邮件入站基础设施）
- 酒店 / 机票比价（独立、已经被做烂的赛道）
- 付费墙、计费、免费版功能门槛

---

## 8. 推荐排期

| 阶段 | 工作量 | 风险 | 发布 | 为什么现在做 |
|---|---|---|---|---|
| 0. 上架拦路问题 | ~1-2 周 | 低 | Web Store v1.0 | 没做完就不能上架 |
| 1. 抓取入口扩展 | ~1-2 周 | 低 | v1.1 | 便宜的差异化；UX 杠杆最大 |
| 2. 旅行规划深度 | ~3-4 周 | 中 | v1.5 | 从剪藏工具升级为规划工具的分水岭 |
| 3. 分享与同步 | ~2-3 周 | 中 | v2.0 | 没有后端也能做的网络效应功能 |

**先把 v1.0 发出去**。后续每个阶段相互独立 —— 按你自己旅行时觉得哪个最有用 / 最好玩来排。

---

## 9. 关键文件清单（按阶段）

- **Phase 0**：`src/background/service-worker.ts`、`manifest.json`、新增 `src/options/`、`src/lib/triton.ts`、`src/lib/places.ts`、`src/popup/App.tsx`（错误 UX）
- **Phase 1**：`src/background/service-worker.ts`（右键菜单 + 快捷键）、新增 `src/lib/youtube.ts`、`src/lib/pdf.ts`、`src/lib/vision.ts`
- **Phase 2**：`src/lib/types.ts`（List + Pin schema 迁移）、`src/lib/pin-store.ts`（migration）、新增 `src/popup/ItineraryView.tsx`、`src/lib/places.ts` 扩展 field mask
- **Phase 3**：新增 `src/lib/share-encode.ts`、`src/popup/ListBar.tsx` 加导入导出按钮、可选 `src/lib/gdrive.ts`

---

## 10. 验收方法

- **Phase 0**：在一个全新的 Chrome profile（不设任何环境变量）里加载未打包扩展。走完引导、填自己的 key、扫一篇已知能用的旅行博客。确认打包后的 `.crx` 里搜不到任何 API key（解压后 grep）。Web Store 草稿提交，确认素材通过审核员检查。
- **Phase 1**：在东京博客里右键 "Bar Trench" → 2 秒内出 pin。打开 YouTube 旅行 vlog → "扫描"从字幕里拉出 5+ 个地点。在旅行文章上工具栏角标显示地点数。
- **Phase 2**：新建一个"东京 3 月 10-15"行程，加 8 个 pin，分配到不同天，点"优化第 2 天" → pin 按距离重排。Pin 弹窗显示评分、营业时间、照片。
- **Phase 3**：点"分享列表" → URL 复制到剪贴板。在装了扩展的隐身窗口打开 → 进入只读视图。点"导入"复制到本地。
